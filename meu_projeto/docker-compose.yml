version: '3'
services:
  web:
    build: 
    ports:
      - "8000:8000"
    volumes:
      - .:/usr/src/app  # Mount your local code into the container
    command: python manage.py runserver 0.0.0.0:8000